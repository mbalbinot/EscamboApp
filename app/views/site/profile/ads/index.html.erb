<!-- /.panel-heading -->
<div class="panel-body">
    <div class="table-responsive">
        <table class="table table-hover">
            <thead>
                <tr>
                    <th>#</th>
                    <th>Imagem</th>
                    <th>Título</th>
                    <th>Categoria</th>
                    <th>Preço</th>
                </tr>
            </thead>
            <tbody>
                <% @ads.each do |ad| %>
                    <tr>
                        <td><%= ad.id %></td>
                        <td><%= image_tag ad.picture, size: "100x100" %></td>
                        <td><%= ad.title %></td>
                        <td><%= ad.category.description %></td>
                        <td><%= humanized_money_with_symbol ad.price %></td>
                    </tr>
                <% end %>
            </tbody>
        </table>
    </div>
    <!-- /.table-responsive -->
</div>
<!-- /.panel-body -->
